--- @brief 多world 全局函数定义

function _G.OnParallelWorldLoadResult(worldID, worldGameInstance, result)
end

function _G.OnParallelWorldFirstFrame(worldID)
end

function _G.OnParallelWorldFocusChanged(oldWorldId, newWorldId)
end

function _G.OnParallelWorldKilled(worldID)
end

function _G.OnParallelWorldGameInstanceChanged(worldID, worldGameInstance)
end